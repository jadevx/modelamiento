version: '3.8'

services:
  frontend:
    build:
      context: ./edo_euler
    ports:
      - "8087:8087"
    depends_on:
      - backend

  backend:
    build:
      context: ./Restaurante
    ports:
      - "5001:5001"
